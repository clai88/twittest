<h1>Make a new tweet</h1>


<%= form_for @tweet, url: tweets_path do |f| %>

<% @tweet.errors.full_messages.each do |msg| %>
  <p>
    <%= msg %>
  </p>
<% end %>

<%= f.label :body %>
<%= f.text_area :body %>

<%= f.submit "Post tweet" %>

<% end %>
